Pass only if the plan/report says both `general` and `secrets-crypto` reviewed the change, no more
than five reviewer agents ran, verification was inline rather than a verifier Agent call, and the
secret-shaped setting is reported or deliberately sent to manual review without revealing its
value.
