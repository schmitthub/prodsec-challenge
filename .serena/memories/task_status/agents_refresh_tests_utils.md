Completed the directory-scoped agent-guide refresh for `tests/utils/`. Updated only `tests/utils/AGENTS.md`; it now documents all four direct Python files and all nine top-level helper functions, explicitly records the absence of constants/classes/methods/fixtures/tests/nested helpers, and includes utility conventions plus the local alias contract. Verified `tests/utils/CLAUDE.md` is a portable sibling symlink with exact target `AGENTS.md`, so no symlink mutation was required. No Python/test code changed; tests were intentionally not run.
