# syft config for the records-api release SBOM (.github/workflows/build.yml).
# The SBOM is generated from requirements.txt only (`syft scan file:requirements.txt`);
# that file is the canonical dependency manifest for this service.
source:
  name: "records-api"
